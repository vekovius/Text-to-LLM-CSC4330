cat > discord/.env.example <<'EOF'
# Copy to .env before running and fill in values.
DISCORD_TOKEN=your-discord-bot-token
# Option 1: point to your project's LLM HTTP endpoint (recommended if you have one)
LLM_SERVER_URL=http://localhost:8000/llm
# Option 2: use OpenAI directly (if you don't run a local LLM)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-3.5-turbo
EOF
